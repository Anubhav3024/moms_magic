import type { Config } from "tailwindcss";

export default {
  content: [
    "./src/pages/**/*.{js,ts,jsx,tsx,mdx}",
    "./src/components/**/*.{js,ts,jsx,tsx,mdx}",
    "./src/app/**/*.{js,ts,jsx,tsx,mdx}",
  ],
  theme: {
    extend: {
      colors: {
        background: "var(--background)",
        foreground: "var(--foreground)",
        primary: {
          DEFAULT: "#F9F6EE", // Cream
          dark: "#F5F1E0",
        },
        secondary: {
          DEFAULT: "#FFC107", // Warm Yellow
          accent: "#FF8C00", // Soft Orange
        },
        brand: {
          red: "#8B0000", // Deep Red
        },
      },
      fontFamily: {
        sans: ["var(--font-outfit)", "ui-sans-serif", "system-ui"],
        display: ["var(--font-playfair)", "serif"],
      },
      borderRadius: {
        xl: "1rem",
        "2xl": "1.5rem",
        "3xl": "2rem",
      },
      boxShadow: {
        warm: "0 4px 20px -2px rgba(255, 193, 7, 0.1), 0 2px 10px -1px rgba(255, 193, 7, 0.05)",
      },
    },
  },
  plugins: [],
} satisfies Config;
